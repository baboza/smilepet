import { NextResponse } from 'next/server'; import { db } from '@/lib/firebase/config'; import { collection, getDocs, query, where } from 'firebase/firestore'; export async function GET() { const snap = await getDocs(query(collection(db, 'appointments'), where('petId', '==', 'Rva3Ue7AgFPD5mv68pMi'))); const docs = snap.docs.map(d => ({id: d.id, ...d.data()})); return NextResponse.json(docs); }
