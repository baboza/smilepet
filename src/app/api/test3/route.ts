import { NextResponse } from 'next/server'; import { db } from '@/lib/firebase/config'; import { collection, getDocs } from 'firebase/firestore'; export async function GET() { const snap1 = await getDocs(collection(db, 'vaccinations')); const snap2 = await getDocs(collection(db, 'parasite_preventions')); const docs1 = snap1.docs.map(d => ({id: d.id, ...d.data()})); const docs2 = snap2.docs.map(d => ({id: d.id, ...d.data()})); return NextResponse.json({vaccines: docs1, parasites: docs2}); }
